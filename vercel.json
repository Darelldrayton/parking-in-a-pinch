{
  "buildCommand": "ls -la && pwd && find . -name 'package.json' -type f | head -10",
  "outputDirectory": ".",
  "installCommand": "echo 'Debug directory structure'",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://165.227.111.160/api/$1"
    }
  ]
}
